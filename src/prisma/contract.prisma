// use prisma-8
model User {
  id        String                     @id @default(uuid())
  name      String
  email     String                     @unique
  password  String

  createdAt temporal.createdAtString()
  updatedAt temporal.updatedAtString()
}
